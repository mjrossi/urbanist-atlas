/**
 * Placeholder home route. Renders inside the App layout shell so the
 * masthead, nav, and footer chrome are visible at /. Replaced by the
 * real two-column broadsheet (SearchBox + drop-cap lede + browse +
 * recently-added) in slice #11.
 */
export function Home() {
  return (
    <div className="page">
      <p>Atlas — coming soon</p>
    </div>
  );
}
