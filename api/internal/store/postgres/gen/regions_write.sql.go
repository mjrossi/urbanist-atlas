// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: regions_write.sql

package gen

import (
	"context"
)

const getRegionIDBySlug = `-- name: GetRegionIDBySlug :one
SELECT id FROM regions WHERE slug = $1
`

func (q *Queries) GetRegionIDBySlug(ctx context.Context, slug string) (int64, error) {
	row := q.db.QueryRow(ctx, getRegionIDBySlug, slug)
	var id int64
	err := row.Scan(&id)
	return id, err
}

const upsertRegion = `-- name: UpsertRegion :one

INSERT INTO regions (kind, name, slug, country, scope_tier)
VALUES ($1, $2, $3, $4, $5)
ON CONFLICT (slug) DO UPDATE
    SET kind       = EXCLUDED.kind,
        name       = EXCLUDED.name,
        country    = EXCLUDED.country,
        scope_tier = EXCLUDED.scope_tier
RETURNING id
`

type UpsertRegionParams struct {
	Kind      string
	Name      string
	Slug      string
	Country   string
	ScopeTier string
}

// Write queries against the regions table. Used by the `loadpostal`
// subcommand to ingest postal-code crosswalks idempotently.
//
// The slug column carries the UNIQUE constraint so it is the natural
// conflict target; an UPDATE refreshes the rest of the row so a
// corrected name/kind/scope_tier in the source file flows through to
// the DB on the next run.
func (q *Queries) UpsertRegion(ctx context.Context, arg UpsertRegionParams) (int64, error) {
	row := q.db.QueryRow(ctx, upsertRegion,
		arg.Kind,
		arg.Name,
		arg.Slug,
		arg.Country,
		arg.ScopeTier,
	)
	var id int64
	err := row.Scan(&id)
	return id, err
}
